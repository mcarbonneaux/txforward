// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("txforward", "for txforward support", "no");

// Otherwise, use ARG_ENABLE
ARG_ENABLE("txforward", "enable txforward support", "no");

if (PHP_TXFORWARD != "no") {
	EXTENSION("txforward", "txforward.c");
}

